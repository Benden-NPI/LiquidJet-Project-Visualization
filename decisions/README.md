# Decisions (ADR)

採用輕量級 **Architecture / Project Decision Records**。每個重要決策都留一份不可變紀錄，後續變動以新 ADR 取代或附加。

- `_template.md` — ADR 模板
- 命名：`NNNN-<short-title>.md`（4 位數遞增），例：`0001-use-mermaid-for-mindmap.md`
